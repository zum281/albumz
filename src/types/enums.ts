export enum MediaType {
  DIGITAL = "digital",
  PHYSICAL = "physical",
}

export enum Source {
  SCAN = "scan",
  MANUAL = "manual",
  IMPORT = "import",
}

export enum AlbumListenedToggle {
  ALL = "all",
  PLAYED = "played",
  BACKLOG = "backlog",
}
