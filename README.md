# Albumz

I quit Spotify and wanted something to track/rate my music. This scans a (for now) hardcoded dir path, then lists albums, and lets you rate them and mark as listened.

## Quick start

```sh
pnpm install
pnpm tauri dev
```

Build:

```sh
pnpm tauri build
```
